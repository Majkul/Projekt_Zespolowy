@{
    ViewData["Title"] = "Oferta nie istnieje";
}

<div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
        <div class="card card-md">
            <div class="card-body">
                <div class="empty">
                    <div class="empty-icon"><i class="bi bi-search fs-1 text-secondary"></i></div>
                    <p class="empty-title">Oferta nie istnieje</p>
                    <p class="empty-subtitle text-secondary">Nie znaleziono oferty o podanym adresie.</p>
                    <div class="empty-action">
                        <a asp-action="Index" class="btn btn-primary">Wróć do ogłoszeń</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
